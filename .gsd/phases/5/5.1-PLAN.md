# Plan 5.1: UI/UX Final Polish

## 1. System Tray & Core Changes
- [x] Update `App.xaml.cs` to add the "About" context menu ("Made by ghuy").
- [x] Update `App.xaml.cs` to remove the ResultWindow message boxes (except errors).

## 2. Settings Data Model
- [x] Add UX properties to `AppSettings.cs` (`Theme`, `OverlayPosition`, `Opacity`, `Scale`, `FontSize`, `ShowTimer`).
- [x] Create Enums for `OverlayPosition` and `AppTheme`.

## 3. Configuration UI
- [x] Update `ConfigWindow.xaml` building the exact UI groupbox specified in the user's reference image.
- [x] Update `ConfigViewModel.cs` to map to these AppSettings values.

## 4. Visual Execution Polish
- [x] Create `LoadingIndicatorWindow.xaml` (transparent, blinking green checkmark, center screen/specific position).
- [x] Delete `LoadingWindow.xaml`.
- [x] Update `App.xaml.cs` to show `LoadingIndicatorWindow` during AI fetch.
- [x] Update `ResultWindow.xaml` to match the screenshot UI (translucent dark background, minimal padding).
- [x] Update `ResultWindow.xaml` to bind layout transforms for `Scale`, opacity binds, and text font sizes.
- [x] Update `ResultWindow.xaml.cs` to parse `OverlayPosition` from settings if needed.
- [x] Attach `DispatcherTimer` to `ResultWindow` to tick seconds counting upward with a stopwatch icon.
