# Plan 1.3 Summary

- **Objective**: Build the main configuration window using MaterialDesignInXAML.
- **Completed Tasks**:
  1. Built `ConfigWindow.xaml` featuring a `TabControl` with two tabs (API Keys, Presets).
  2. Created `ConfigViewModel` that wires up `SettingsManager` and exposes inputs via `ObservableObject` and `RelayCommand`.
  3. Modified `App.xaml.cs` to allow opening the configuration window from the system tray menu. Added window single-instance tracking logic.
  4. Configured `App.xaml` to merge `MaterialDesignThemes` resource dictionaries.
  5. Fixed missing project reference between UI and Core projects.
- **Verification**: Built successfully.
- **Done**: The tray icon can now launch a functional ConfigWindow.
