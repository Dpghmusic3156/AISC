<Window x:Class="AIScreenCapture.UI.Views.ResultWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:md="clr-namespace:MdXaml;assembly=MdXaml"
        mc:Ignorable="d"
        Title="AI Result" Height="500" Width="600"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent" Topmost="True" ResizeMode="CanResizeWithGrip"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="14"
        TextOptions.TextFormattingMode="Ideal" 
        TextOptions.TextRenderingMode="Auto"        
        FontFamily="{DynamicResource MaterialDesignFont}">
    
    <materialDesign:Card Margin="10" UniformCornerRadius="8" materialDesign:ElevationAssist.Elevation="Dp4" Background="{DynamicResource MaterialDesignPaper}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Header for dragging and closing -->
            <Border Grid.Row="0" Background="{DynamicResource PrimaryHueMidBrush}" CornerRadius="8,8,0,0" Padding="16,8" MouseLeftButtonDown="Header_MouseLeftButtonDown">
                <Grid>
                    <TextBlock x:Name="TitleText" Text="AI Result" Foreground="{DynamicResource PrimaryHueMidForegroundBrush}" Style="{StaticResource MaterialDesignHeadline6TextBlock}" VerticalAlignment="Center" FontSize="16"/>
                    <Button HorizontalAlignment="Right" Style="{StaticResource MaterialDesignIconForegroundButton}" ToolTip="Close" Click="CloseButton_Click" Height="30" Width="30" Padding="0">
                        <materialDesign:PackIcon Kind="Close" Width="20" Height="20" Foreground="{DynamicResource PrimaryHueMidForegroundBrush}"/>
                    </Button>
                </Grid>
            </Border>
            
            <!-- Body for Markdown rendering -->
            <Border Grid.Row="1" Padding="16">
                <!-- MdXaml renders standard markdown. It contains an internal ScrollViewer, so we don't nest it unless needed -->
                <md:MarkdownScrollViewer x:Name="MarkdownViewer" Markdown="Loading response..." Margin="0,0,0,10" />
            </Border>
            
            <!-- Footer for actions -->
            <Border Grid.Row="2" Background="{DynamicResource MaterialDesignToolBarBackground}" CornerRadius="0,0,8,8" Padding="16,8">
                <Grid>
                    <TextBlock x:Name="NotificationText" Text="Copied to clipboard!" Foreground="Green" VerticalAlignment="Center" Visibility="Hidden" />
                    <Button HorizontalAlignment="Right" Content="Copy Text" Click="CopyButton_Click" Style="{StaticResource MaterialDesignFlatButton}" Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                </Grid>
            </Border>
        </Grid>
    </materialDesign:Card>
</Window>
