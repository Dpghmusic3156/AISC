<Window x:Class="AIScreenCapture.UI.Views.ResultWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="AI Result" SizeToContent="WidthAndHeight" MaxWidth="450" MaxHeight="600"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent" Topmost="True" ResizeMode="NoResize" ShowInTaskbar="False"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="14"
        TextOptions.TextFormattingMode="Ideal" 
        TextOptions.TextRenderingMode="Auto"        
        FontFamily="{DynamicResource MaterialDesignFont}">
    
    <Grid>
        <Grid.LayoutTransform>
            <ScaleTransform x:Name="WindowScale" ScaleX="1.0" ScaleY="1.0"/>
        </Grid.LayoutTransform>
        
        <Border x:Name="MainBorder" Margin="5" CornerRadius="4" Background="#F2FFFFFF" HorizontalAlignment="Left" VerticalAlignment="Top">
            <!-- Subtle Web-like shadow -->
            <Border.Effect>
                <DropShadowEffect BlurRadius="4" ShadowDepth="1" Opacity="0.1" Color="Black" />
            </Border.Effect>
            <Grid>
                <!-- Body for Markdown rendering -->
                <Border Padding="8,6" Background="Transparent" CornerRadius="4">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" MaxHeight="550">
                        <TextBlock x:Name="ResultText" Text="Loading..." TextWrapping="Wrap" MaxWidth="450" LineHeight="18" />
                    </ScrollViewer>
                </Border>
                
                <!-- Sticky Notification Layer -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="16,0,16,16" IsHitTestVisible="False">
                     <TextBlock x:Name="NotificationText" Text="Copied to clipboard!" Foreground="#4CAF50" FontSize="12" Margin="0,0,8,0" Visibility="Hidden" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
