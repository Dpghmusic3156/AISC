<Window x:Class="AIScreenCapture.UI.Views.LoadingIndicatorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Processing" SizeToContent="WidthAndHeight"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent" Topmost="True" ResizeMode="NoResize"
        ShowInTaskbar="False">
    
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <!-- A simple checkmark that blinks -->
            <materialDesign:PackIcon Kind="Check" Width="20" Height="20" Foreground="#10b981" HorizontalAlignment="Center">
                <materialDesign:PackIcon.Effect>
                    <DropShadowEffect BlurRadius="4" ShadowDepth="0" Color="#10b981" Opacity="0.5"/>
                </materialDesign:PackIcon.Effect>
                <materialDesign:PackIcon.Triggers>
                    <EventTrigger RoutedEvent="Loaded">
                        <BeginStoryboard>
                            <Storyboard RepeatBehavior="Forever" AutoReverse="True">
                                <DoubleAnimation Storyboard.TargetProperty="Opacity" From="1.0" To="0.3" Duration="0:0:0.5" />
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </materialDesign:PackIcon.Triggers>
            </materialDesign:PackIcon>
            
            <TextBlock x:Name="TimerText" Text="(0s)" Foreground="#10b981" FontWeight="Bold" FontSize="11" HorizontalAlignment="Center" Margin="0,4,0,0" Visibility="Collapsed" />
        </StackPanel>
    </Grid>
</Window>
